const UseEffectHook = () =>{

    const fieldNames = ["text-input", "text-input", "choice-input"]

    const form = fieldNames.map(
        (field, key) => {

        }
    )

    return(<>
            {form}
        </>
    );
}

export default UseEffectHook;